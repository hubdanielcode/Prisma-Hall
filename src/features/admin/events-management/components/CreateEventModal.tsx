interface CreateEventModalProps {
  isOpen: boolean;
  onClose: () => void;
}

const CreateEventModal = ({ isOpen: _isOpen, onClose: _onClose }: CreateEventModalProps) => {
  return <div>CreateEventModal</div>;
};

export { CreateEventModal };
